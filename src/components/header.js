import * as React from "react"
import { Link } from "gatsby"

const Header = () => {
    return (
      <nav>
        <Link to="/"><h1>edgedaily</h1></Link>
      </nav>
    )
  }
  
  export default Header;
